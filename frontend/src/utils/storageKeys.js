import { brand } from '../content/brand';

export const STORAGE_KEYS = {
  token: `${brand.localStoragePrefix}_token`,
  user: `${brand.localStoragePrefix}_user`,
  locale: `${brand.localStoragePrefix}_locale`,
  theme: `${brand.localStoragePrefix}_theme`,
};
